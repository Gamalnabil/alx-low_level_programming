#include "main.h"
#include <math.h>

/**
 * _abs - get the absolute value of the integer
 * 
 * Return: always 0 (Success)
*/
int _abs(int n)
{	
	if (n < 0)
	{
		absolute_value = -n
	}
	else
	{
		absolute_value = n
	}
}
