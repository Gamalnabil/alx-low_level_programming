#include "main.h"

/**
 * _isdigit - check if c is adigit
 * @c: the char of input
 * Return: 1 if the value is digit else return 0
*/
int _isdigit(int c)
{
	int z;
	for (z = 0; z <= 9; z++)
	{
		if (c = z)
		{
			return (1)
		}
		else
		{
			return (0)
		}
	}
}
