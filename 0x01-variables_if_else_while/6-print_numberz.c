#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int m;

	for (m = 0; m <10; m++)
	{
		putcher(m);
	}
	putcher('\n');
	return (0);
}`
