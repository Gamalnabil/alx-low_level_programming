#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int manin(void)
{
	int i = 97;

	for (i <= 122; i++)
	{
		putchar (i);
	}
	putchar ('\n');
	
	return (0);
}
